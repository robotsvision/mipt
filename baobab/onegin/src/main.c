#include <onegin.h>

main_t ellipse_main(app_args_t args) {

    ellipse_init(ELLIPSE_INIT_CONOSOLE);

    /**
     * START GUI MODE IF THERE ARE NO ARGUMENTS
     */

    if (args.num == 0) {
        onegin_gui(args);
        exit(EXIT_SUCCESS);
    }

    /**
     * RECOGNIZE FLAGS
     */

    if ()
    


    return EXIT_SUCCESS;
}