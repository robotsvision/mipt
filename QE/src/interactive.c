/* Include specific libraries */
#include "interactive.h"
#include "qe_config.h"

/* Public functions */

void qe_interactive(void) {
    /* Init SDL2 and create window */
    /*
    SDL_Init(SDL_INIT_VIDEO);
    SDL_CreateWindow(
        X(""),
        SDL_WINDOWPOS_CENTERED,
        SDL_WINDOWPOS_CENTERED,
        QE_INTERACTIVE_WIDTH,
        QE_INTERACTIVE_HEIGHT,
        0
    );
    SDL_Delay(2000);
    SDL_Quit();
    */
}